---
active: true
iteration: 1
session_id: 
max_iterations: 12
completion_promise: "RUBRIC_PERFECT"
started_at: "2026-03-15T20:58:25Z"
---

TASK: Review and revise AI-generated unit tests so they fully comply with the Unit Test Writing Rubric (Contract-first), and keep iterating until every test is rubric-compliant.\n\nRUBRIC TO ENFORCE:\n- Test behavior, not implementation.\n- Prefer contract tests over scaffold tests.\n- If behavior intent is ambiguous, add a  comment instead of silently guessing.\n- Route/controller unit tests must NOT mock ORM/query-builder chains such as select().from().where().\n- Avoid order-dependent mock ladders such as mockReturnValueOnce chains that encode internal query order unless the call order itself is the explicit boundary contract.\n- Do not simulate Promise internals or thenable objects to fake query chaining.\n- No suite-shape tripwires as contract tests, such as asserting export counts or tool counts, unless explicitly labeled .\n- Every test must clearly state what regression it would catch through its name and assertions.\n- If a unit touches database logic, introduce or use a repository/service seam and mock that seam instead of mocking the ORM.\n- If a seam cannot yet be introduced, the test must be labeled  and described as temporary characterization.\n- Use clear AAA structure.\n- Prefer observable contracts: status codes, response bodies, returned values, emitted events, persisted data.\n- Prefer stable assertions over brittle exact string matching unless the string is itself part of the public contract.\n- Parse payloads and assert meaningful structured fields instead of vague string contains checks.\n- Non-mutating behavior must be asserted explicitly when relevant.\n- Mock only at true system boundaries.\n- Remove unused mocks.\n- Boundary assertions must verify meaningful payloads, not just that something was called.\n- Call counts are allowed only when the count itself is part of the boundary contract.\n- Cover happy paths, validation, authorization/security, edge cases, error handling, and state transitions where applicable.\n- Use deterministic timers. No sleeps.\n- Avoid real network and real database access in unit tests.\n- Label scaffold tests with .\n- Output coverage summary, REVIEW flags, and suggested integration tests where unit tests cannot prove query correctness.\n\nPROCESS:\n1. Inspect the current unit tests and identify every rubric violation.\n2. Revise the tests to fix the highest-signal violations first.\n3. For any route/controller tests that mock ORM or query-builder chains, replace those mocks with repository or service seam mocks. If needed, create the seam with minimal production changes that preserve behavior.\n4. For any ambiguous spec, add explicit  comments instead of guessing.\n5. Label any unavoidable characterization tests as .\n6. Remove brittle assertions, unused mocks, fake thenables, order-dependent mock ladders, and suite-shape tripwires unless explicitly justified by boundary contract.\n7. Run the relevant tests, linting, and any targeted validation after each change.\n8. Re-check the revised tests against the rubric and continue iterating until all criteria are satisfied.\n9. Before finishing, self-score each changed test file from 0 to 2 on these axes and explain any 0s: contract stability, mock quality, assertion meaning, flake risk, spec clarity.\n10. If blocked after repeated attempts, summarise blockers, attempted fixes, and the smallest next seam or refactor needed.\n\nSUCCESS CRITERIA:\n- All changed unit tests pass.\n- No changed route/controller unit test mocks ORM or query-builder chains.\n- No changed tests use fake thenables or Promise-internal simulation.\n- No changed tests rely on order-dependent mock ladders unless explicitly documented as boundary-contract behavior.\n- Any scaffold or characterization test is explicitly labeled .\n- Ambiguous intent is marked with  comments instead of being guessed.\n- Assertions verify observable behavior and meaningful payloads.\n- Unused mocks are removed.\n- Deterministic timing is used where relevant.\n- Coverage summary is included for the changed tests.\n- Suggested integration tests are listed where unit tests cannot verify query correctness or dedup semantics.\n- A self-review score is included for each changed test file.\n\nOUTPUT:\nOutput <promise>RUBRIC_PERFECT</promise> only when ALL success criteria are met.\nIf still blocked after 12 iterations, output <promise>BLOCKED</promise> and include blockers, attempted fixes, and next-step recommendations.
