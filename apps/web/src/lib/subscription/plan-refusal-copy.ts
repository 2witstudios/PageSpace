/**
 * Plan-gate refusals the server sends and the UI shows verbatim — on every
 * platform, the iOS app included, so they state the rule without a purchase
 * call to action (Guideline 3.1.1).
 */
export const CUSTOM_DOMAINS_UNAVAILABLE_MESSAGE = 'Custom domains are available on Pro and higher plans.';
export const CUSTOM_SUBDOMAIN_UNAVAILABLE_MESSAGE = 'Custom subdomain selection is available on Pro and higher plans.';
