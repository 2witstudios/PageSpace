apiVersion: 1
datasources:
  - name: InfluxDB_k6
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: pagespace
      defaultBucket: k6
    secureJsonData:
      token: "${INFLUXDB_TOKEN}"
