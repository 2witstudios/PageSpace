# Spec ID-coverage allowlist — IDs from the Organizations & Wallets Spec (page
# drc7x34unhc0ty1dc0u1j3gy) that have no test yet because their lane has not landed.
#
# Rules (enforced by scripts/check-spec-coverage.ts):
#   - Every ID started allowlisted when the gate was introduced (Wave A, lane A4).
#   - A lane REMOVES its IDs from this file in the same PR that lands the tests naming them
#     ("MON-2 …" in an it/test title, or a describe title whose tests ALL pass — never only in a
#     riteway given/should, which no reporter sees). Only test runs in .github/workflows/ci.yml
#     count: a suite only security.yml runs never reaches the gate. Leaving an ID here
#     after a test names it FAILS the gate (STALE-ALLOW), so this file shrinks as work lands.
#   - A token here that is not a Spec ID FAILS the gate (typo guard).
#   - Re-adding an ID is allowed ONLY when a review shows the test(s) covering it do not prove
#     the requirement (an over-claimed title). That PR removes the ID from those titles, re-adds
#     it here with a comment saying why, and its PR body cites the review. Never re-add an ID to
#     silence a gap: if a test that genuinely proved a requirement is deleted, the gate goes red,
#     and that is the point. The STALE-ALLOW failure above is unchanged.
#   - A test that proves only PART of a requirement titles it "<ID> (partial) …" (the marker
#     immediately after the ID, e.g. "MON-3 (partial) grantForInvoiceLines sums base and
#     extra-seat lines before the ratio"; review 5230424182 on #2657). The gate lists such tests
#     under the ID for traceability but never counts them: an ID is covered only when some passing
#     test names it WITHOUT the marker, and only those hits make an entry here STALE-ALLOW. So a
#     partially proven ID stays on this list, with a comment naming what is still unproven.
# One ID per line; `#` starts a comment.
AUD-1
AUD-2
AUD-3
DRV-3
# DRV-5 (Open: every org member resolves as an implicit member with the drive's default role; sidebar,
# picker and accessible-drives include it): B4's org-membership-sync tests prove the materialized rows
# (default role, accepted, drive lead excluded, idempotent), titled "DRV-5 (partial)"; resolution through
# the permission resolver and inclusion in sidebar, picker and accessible-drives are unproven until B7/Wave F.
DRV-5
DRV-6
DRV-7
# DRV-8 (a guest is a drive member not in the org; guests hold no seat, see only the drives they were
# invited to, and are labeled as guests) re-added per reviews 5230157358 and 5230240628 on #2659:
# the B1 tests prove only that drive_members.source distinguishes invited rows from org-materialized
# rows, so they are titled "DRV-8 (partial)"; seat exclusion, visibility and labeling are unproven
# until B4/B7, Wave C and Wave F.
DRV-8
DRV-9
# MON-3 (the org pool refill: (Business base + extra-seat items) paid × ratio) re-added per review
# 5230424182 on #2657: invoice-grant.test.ts proves the grantForInvoiceLines arithmetic, but that
# seam has no production caller and no org pool exists, so nothing is refilled yet. Those tests
# are titled "MON-3 (partial)"; Phase 3 D1 proves the refill.
MON-3
# ORG-1 (name, slug, avatar, one human Owner; ownership is transferable), ORG-2 (Owner/Admin/Member;
# many orgs per user; a user in no org sees no org UI) and ORG-3 (email invites with expiry, resend,
# revoke, acceptance for new and existing accounts; a pending invite counts as a seat) re-added per
# reviews 5230157358 and 5230240628 on #2659: the B1 schema tests prove only the storage shape
# (fields, the one-OWNER-row cap, unique membership, one open case-insensitive invite), so they are
# titled "(partial)". B3 (org services and routes) then proved Owner membership matching ownerId
# through creation and transfer, roles and membership across many orgs, and the invite lifecycle
# (expiry, rotation after expiry, resend, revoke, acceptance by a new or an existing account, live-
# invite seat counting), still titled "(partial)" because each test proves one clause. Still
# unproven: ORG-1 "human" Owner (agent accounts), ORG-2 the no-org-UI rule (Wave F), ORG-3 invite
# acceptance wired into every sign-up flow (Wave F accept page) and a pending invite counting in
# the Stripe seat quantity (Wave C, SEAT-3).
ORG-1
ORG-2
ORG-3
ORG-4
# ORG-5 (one shared org permission function used by web routes, CLI, and MCP): B3 routes all go
# through requireOrgRole, titled "ORG-5 (partial)"; CLI and MCP use it in Wave G (X-1).
ORG-5
POL-1
POL-2
POL-3
POL-4
POL-5
POL-6
# POL-7 (partial, lane ow-c2 wallet-core): effectiveSpendPolicy proves "a drive may only be stricter" as pure
# math; the stored policy and the credit gate reading it are unproven until E1/C3.
POL-7
POL-8
POL-9
POL-10
POL-11
SEAT-1
# SEAT-2 (tier table: Free, Pro, Business $50 with 5 seats + $10/extra seat; Business is the org
# plan — buying it creates the org — and is not sold to a lone user) re-added per review
# 5230424182 on #2657: the tier table, prices and lone-user refusal are proven, so those tests are
# titled "SEAT-2 (partial)"; "buying it creates the org" is unproven until Wave C creates orgs.
SEAT-2
# SEAT-3: B3's countOrgSeats counts accepted members plus live invites only ("SEAT-3 (partial)");
# the Stripe quantity is Wave C.
SEAT-3
SEAT-4
SEAT-5
SEAT-6
SEAT-7
SEAT-8
SEAT-9
SEC-1
SEC-2
SEC-3
SEC-4
# SPEND-1 (partial, ow-c2): resolveSpendSource picks exactly one source; that every AI call in a drive
# goes through it before the call is unproven until C3.
SPEND-1
SPEND-2
SPEND-3
# SPEND-4 (partial, ow-c2): empty chosen source refuses, names it, offers the rest, charges zero, and
# falls back only by drive rule, as pure logic; the chip and strip showing the new source are unproven (UI-8).
SPEND-4
# SPEND-5 (partial, ow-c2): both override switches are absolute in resolveSpendSource; storing them per
# drive and globally is unproven.
SPEND-5
# SPEND-6 (partial, ow-c2): automations resolve to the drive wallet or skip, never a person; wiring every
# automation kind and logging the skip are unproven until C4.
SPEND-6
SPEND-7
SPEND-8
SPEND-9
SPEND-10
UI-1
UI-2
UI-3
UI-4
UI-5
UI-6
UI-7
UI-8
UI-9
UI-10
UI-11
# WAL-1 (partial, ow-c1): the wallets table carries every WAL-1 field, the migration proves each
# former credit_balances row became its user's personal root wallet to the cent, and the constraint
# suite proves owner/subject/parent/status shapes; top-up "never expires" on child wallets is unproven
# until C3/C5 spend and refill against them.
WAL-1
# WAL-2 (partial, ow-c1): one pool per org, one wallet per subject and a drive wallet under a parent
# are enforced by the schema; creating the pool, drive wallets and seat caps is C5/C6.
WAL-2
# WAL-3 (partial, ow-c2): allocation draws against the parent as spend happens and legs last until spent,
# as pure math; persisted wallets and ledger rows are unproven until C1/C3.
WAL-3
WAL-4
# WAL-5 (every AI usage row records the wallet charged; holds per wallet; ledger, reconciliation
# and monthly reset keyed on wallet) re-added by lane ow-r0-fixups per Review 0b (page
# wiv716vr39v9wbx2z40aoio1, Follow-up 2): no wallet exists yet, and the tests that named it
# prove user-keyed missed-grant reconciliation (MON-2), not wallet keying. Their titles no
# longer claim it. Remove this line once a test exercises wallet-keyed rows.
# ow-c1: usage rows now record the wallet charged, every ledger and hold row names its wallet (NOT
# NULL, backfilled), and cost reconciliation corrects the wallet the charge was billed to — each
# titled "WAL-5 (partial)". Still user-keyed: the gate's monthly reset and settle lock the personal
# root by user (identical while every wallet charged is a personal root); C3 keys them on wallet.
WAL-5
# WAL-6 (partial, ow-c2): overshoot landing by funder choice, over state, once-per-period notice and
# debt netting are pure logic; the hold/settle path, forgiveness at renewal and the notification are unproven.
WAL-6
# WAL-7 (partial, ow-c2): UTC caps, defaults and 80/100% thresholds are pure logic; caps on stored legs,
# alert delivery and the kill switch control are unproven until C3/C6.
WAL-7
WAL-8
WAL-9
X-1
# X-2: B3's "X-2 (partial)" test blocks ORGS_ENABLED while org tables are excluded from the GDPR
# export; the export and account-deletion handling itself is the Phase 6 leaf yfmlkdchehmberwthwu6g7vt.
X-2
X-3
# X-4 (realtime events for membership, visibility and wallet state): B4 proves one drive-member event
# per affected user from the org membership sync, titled "X-4 (partial)"; wallet-state events and the
# client updating without refresh are unproven until Wave C/F.
X-4
# X-6 (Negative tests: non-member sees no other org's Open drive; a guest sees exactly one
# drive; a disabled policy refuses; an empty chosen wallet charges nothing; an automation never
# charges a person) needs org/wallet resolution logic that does not exist until Wave B/C lands.
# The seam guards and the Northwind fixture shape check in this lane are real tests but they
# check code STRUCTURE, not these five authorization/spend behaviors — they do not prove X-6 and
# their names no longer claim it (PR #2642 review thread PRRT_kwDOPhnPxc6ioz-F). Only remove this
# line once a test exercises one of the five behaviors above against real resolution logic.
X-6
