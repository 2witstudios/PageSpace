# Copy to .env.local. Both values are public (no secret exists for a public client).
# The local PageSpace web app (http is accepted for a loopback base URL only).
VITE_PAGESPACE_URL=http://localhost:3000
# The client_id seeded by seed-client.sql.
VITE_PAGESPACE_CLIENT_ID=signin-smoke
