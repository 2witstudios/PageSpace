name: Test Suite

on:
  push:
    branches: [main, master, develop, pu/flash-sandbox, pu/panes-agent, pu/broken-sessions]
  pull_request:
    branches: [main, master, develop, pu/flash-sandbox, pu/panes-agent, pu/broken-sessions]
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
