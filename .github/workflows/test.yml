name: Test Suite

on:
  push:
    branches: [main, master, develop, pu/flash-sandbox]
  pull_request:
    branches: [main, master, develop, pu/flash-sandbox]
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
